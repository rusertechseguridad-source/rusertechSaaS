export interface OperativosSettings {
  vehicle_moving_min_speed_kmh: number;
  location_default_radius_meters: number;
  destination_arrival_proximity_km: number;
  broken_loop_threshold_minutes: number;
  provider_ok_threshold_minutes: number;
}
